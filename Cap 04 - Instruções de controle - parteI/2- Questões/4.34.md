## 4.34 — O que há de errado com esse código?

O que há de errado com a seguinte instrução? Forneça a instrução correta para adicionar 1 à soma de `x` e `y`.

```java
System.out.println(++(x + y));
```