# 2.8 — Escreva instruções Java


a) Exibir a mensagem `"Enter an integer: "` mantendo o cursor na mesma linha.
```Java
System.out.print("Enter an integer: ");
```

b) Atribuir para `a` o produto das variáveis `b` e `c`.
```Java
a = b * c;
```

c) Escrever um comentário dizendo que o programa faz um cálculo de exemplo de folha de pagamento.
```Java
//Calculo de exemplo de folha de pagamento
```